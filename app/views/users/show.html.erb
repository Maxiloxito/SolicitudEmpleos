<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

<title>Perfil de Usuario</title>

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    font-family: "Poppins", sans-serif;
    background-color: #f8f9fa;
    color: rgb(85, 85, 85); 
  }

  .header {
    background-color: #343a40;
    color: #fff;
    padding: 1rem;
  }

  .container {
    margin: 2rem auto;
    max-width: 600px;
  }

  .profile-section {
    background-color: #fff; 
    padding: 2rem;
    border-radius: 1rem;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
  }

  .profile-info {
    margin-bottom: 1rem;
  }

  .profile-info strong {
    font-weight: 600;
  }

  .edit-button {
    background-color: rgb(53, 53, 53);
    border-color: rgb(53, 53, 53); 
    color: #fff; 
    padding: 0.5rem 1rem;
    border-radius: 1rem;
    text-decoration: none;
  }

  .edit-button:hover {
    background-color: rgb(0, 0, 0); 
  }

  .back-button {
    background-color: #fff;
    border-color: rgb(163, 163, 163); 
    color: rgb(53, 53, 53);
    padding: 0.5rem 1rem;
    border-radius: 1rem;
    text-decoration: none;
    display: block;
    margin-top: 1rem;
  }

  .back-button:hover {
    background-color: rgb(53, 53, 53);
    color: #ffff;
}

</style>
</head>
<body>

<div class="container">
  <div class="header">
    <h1>Perfil de Usuario</h1>
  </div>
  
  <div class="profile-section">
    <p class="profile-info"><strong>Nombre:</strong> <%= @user.first_name %> <%= @user.last_name %></p>
    <p class="profile-info"><strong>Email:</strong> <%= @user.email %></p>
    <p class="profile-info"><strong>Ubicación:</strong> <%= @user.location %></p>
    <p class="profile-info"><strong>Contacto:</strong> <%= @user.contact %></p>
    <p class="profile-info"><strong>Tipo de Ingeniería:</strong> <%= @user.engineering_type %></p>
    <p class="profile-info"><strong>Experiencia Laboral:</strong> <%= @user.experience %> años</p>
    <p class="profile-info"><strong>Idiomas:</strong> <%= @user.languages.join(",") %></p>

    <% if @user.resume.attached? %>
      <p class="profile-info"><strong>Curriculum:</strong> <%= link_to "Ver CV", url_for(@user.resume) %></p>
    <% end %>
  
    <%= link_to 'Editar Perfil', edit_user_path(@user), class: 'edit-button' %>
    <%= link_to 'Volver', dashboard_path, class: 'back-button' %>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>