<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= @job_offer.title %></title>
    <!-- Utilizamos Bootstrap 5 como en la página de edición de perfil -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8f9fa;
            color: rgb(85, 85, 85);
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .container {
            background-color: #fff;
            padding: 2rem;
            margin-top: 2rem;
            margin-bottom: 2rem;
            border-radius: 0.25rem;
            box-shadow: 0 .5rem 1rem rgba(0, 0, 0, .15);
        }

        h1.display-4 {
            font-weight: 600;
            color: #343a40;
            border-bottom: 2px solid #000;
            padding-bottom: 0.5rem;
            margin-bottom: 1.5rem;
        }

        .btn-primary {
            background-color: rgb(53, 53, 53);
            border-color: rgb(53, 53, 53);
        }

        .btn-primary:hover {
            background-color: rgb(0, 0, 0);
        }

        .btn-secondary {
            background-color: #fff;
            color: rgb(53, 53, 53);
            border-color: rgb(163, 163, 163);
        }

        .btn-secondary:hover {
            background-color: rgb(53, 53, 53);
            color: #fff;
        }

        .back-link {
            color: #fff;
            background-color: #6c757d;
            text-decoration: none;
            padding: 0.375rem 0.75rem;
            border-radius: 0.25rem;
            transition: color 0.15s ease-in-out, background-color 0.15s ease-in-out, border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
        }

        .back-link:hover {
            color: #fff;
            background-color: rgb(53, 53, 53);
            border-color: rgb(163, 163, 163);
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="display-4"><%= @job_offer.title %></h1>

        <table class="table table-hover">

            <tbody>
                <tr>
                    <th scope="row">Cargo</th>
                    <td><%= @job_offer.position %></td>
                </tr>            
                <tr>
                    <th scope="row">Descripción</th>
                    <td><%= @job_offer.description %></td>
                </tr>
                <tr>
                    <th scope="row">Idioma</th>
                    <td><%= @job_offer.languages.join(", ") %></td>
                </tr>
                <tr>
                    <th scope="row">Salario:</th>
                    <td>$<%= @job_offer.salary %></td>
                </tr>
            </tbody>
        </table>

        <table class="table table-danger">
            <tbody>
                <tr>
                    <th scope="row">Disponibilidad:</th>
                    <td><%= @job_offer.availability %></td>
                </tr>
            </tbody>
        </table>

        <div class="mt-3">
            <%= link_to "Editar", edit_job_offer_path(@job_offer), class: "btn btn-primary" %> |
            <%= link_to "Volver", job_offers_path, class: "btn btn-secondary back-link" %>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
