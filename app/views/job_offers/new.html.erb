<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nueva Oferta de Trabajo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        body {
            font-family: "Poppins", sans-serif;
            background-color: #f8f9fa;
            color: rgb(85, 85, 85);
        }

        .container {
            background-color: #fff;
            padding: 2rem;
            border-radius: .5rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-top: 2rem;
        }

        h1 {
            color: #343a40;
            margin-bottom: 1.5rem;
        }

        .alert {
            background-color: #f8d7da;
            color: #721c24;
        }

        .form-control {
            color: rgb(85, 85, 85);
        }

        .form-control:focus {
            border-color: rgb(53, 53, 53);
            box-shadow: 0 0 0 0.2rem rgba(53, 53, 53, 0.25);
        }

        .btn-primary {
            background-color: rgb(53, 53, 53);
            border-color: rgb(53, 53, 53);
        }

        .btn-primary:hover {
            background-color: rgb(0, 0, 0);
        }

        .btn-secondary {
            background-color: #fff;
            border-color: rgb(163, 163, 163);
            color: rgb(53, 53, 53);
        }

        .btn-secondary:hover {
            background-color: rgb(53, 53, 53);
            color: #fff;
        }

        .form-icon {
            position: relative;
        }

        .form-icon i {
            position: absolute;
            left: 10px;
            top: 50%;
            transform: translateY(-50%);
            color: rgb(53, 53, 53);
        }

        .form-icon input, .form-icon select {
            padding-left: 2.5rem;
        }

        .help-block {
            display: block;
            margin-top: .25rem;
            margin-left: .25rem;
            color: #d9534f;
        }
    </style>
</head>
<body>

<div class="container">
    <h1>Crear Nueva Oferta de Trabajo</h1>
    <% if @job_offer.errors.any? %>
        <div class="alert alert-danger" role="alert">
            <%= pluralize(@job_offer.errors.count, "error") %> impidieron que la oferta de trabajo se guardara:
            <ul>
                <% @job_offer.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <%= form_for @job_offer, html: { class: 'form' } do |f| %>
        <div class="form-group">
            <%= f.label :title, "Título" %>
            <%= f.text_field :title, class: "form-control" %>
        </div><br>

        <div class="form-group">
            <%= f.label :position, "Cargo" %>
            <%= f.select :position, ['Ingeniería civil informática', 'Ingeniería civil industrial','Ingeniería civil ambiental','Ingeniería civil metalurgia','Ingeniería civil electronica'], {}, class: 'form-control' %>
        </div><br>
        <div class="form-group">
            <%= f.label :description, "Descripción" %>
            <%= f.text_area :description, class: "form-control" %>
        </div><br>

        <div class="form-group">
            <%= f.label :location, "Locacion" %>
            <%= f.text_area :location, class: "form-control" %>
        </div><br>

        <div class="form-group">
            <%= f.label :languages, "Idiomas" %><br>
            <% ['Español', 'Inglés', 'Ruso', 'Portugues', 'Japonés'].each do |language| %>
                <%= check_box_tag 'job_offer[languages][]', language, @job_offer.languages&.include?(language), id: sanitize_to_id(language) %>
                <%= label_tag sanitize_to_id(language), language %><br>
            <% end %>
        </div><br>


        <div class="form-group">
            <%= f.label :salary, "Salario" %>
            <%= f.number_field :salary, class: "form-control" %>
        </div><br>

        <div class="form-group">
            <%= f.label :availability, "Disponibilidad" %>
            <%= f.select :availability, JobOffer::STATUS_OPTIONS.keys, { include_blank: "Seleccionar" }, class: "form-control" %>
        </div><br>

        <div class="text-center">
            <button type="submit" class="btn btn-primary">Crear Oferta</button>
            <%= link_to "Volver", dashboard_path, class: "btn btn-secondary" %>
        </div>

    <% end %>
</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
